---
const {frase, source, alternate, position} = Astro.props;

---

<article class=`image-carousel img${position}`>
    <img src={source} alt={alternate}>
    <h1>{frase}</h1>
</article>

<style>

    .image-carousel{
        width: 60vw;
        height: calc(60vw / 1.7);
        position: relative;
        & img{
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1;
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 25px;
            box-shadow: 0 0 100px 2px rgba(80, 103, 129, 0.5);

        }
        & h1{
            position: absolute;
            z-index: 2;
            color: #ff8038;
            font-family: quicksand;
            font-size: 3rem;
            top: -3rem;
            left: -3rem;
            transform:  rotate(-15deg);
            padding: 0.5rem 1rem;
            border-radius: 15px;       
            background-color: #1d1d1d;
            box-shadow: 0 0 20px 2px #1d1d1d;

 text-shadow: 0 0 5px #000;
            white-space: nowrap;
            transform-origin: top right;
        }
    }
</style>